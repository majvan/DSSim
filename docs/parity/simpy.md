# SimPy Parity

DSSim includes parity examples to compare behavior and API style against SimPy.

See:

- `examples/parity/simpy`
- benchmark scripts under `benchmarks/`

Use parity examples to validate scheduling/resource semantics during migration.
